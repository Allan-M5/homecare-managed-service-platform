# Day 3 Auth and Worker Application

Added:
- JWT token utility
- protect middleware
- authorize middleware
- register client endpoint
- register admin endpoint
- login endpoint
- current user endpoint
- worker application submission endpoint

Mounted routes:
- /api/auth
- /api/worker-applications

Next:
- request validation
- MongoDB live connection
- initial frontend auth wiring
- admin review endpoints for worker applications